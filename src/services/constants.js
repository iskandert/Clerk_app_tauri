const categoryStatusEnum = {
    INCOME: 'income',
    EXPENSE: 'expense',
};

const categoryTypeEnum = {
    DEFAULT: 'default',
    SAVINGS: 'savings',
};

const errorEnum = {
    IS_NOT_FOUND: 'isNotFound',
    IS_VALIDATION: 'isValidation',
    IS_INTERNAL: 'isInternal',
};

export { categoryStatusEnum, categoryTypeEnum, errorEnum };
